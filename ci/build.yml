---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.13.8

inputs:
  - name: dp-search-api
    path: dp-search-api

outputs:
  - name: build

run:
  path: dp-search-api/ci/scripts/build.sh
